[package]
name = "zk-soroban"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["lib"]

[dependencies]
soroban-sdk = { workspace = true }
ethnum = {workspace = true}
zk-core = { workspace = true }
